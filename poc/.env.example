# Copy this file to `.env` (or export the variable in your shell) and add your key.
# Get a free key at https://console.groq.com/keys
# The app and smoke test read GROQ_API_KEY from the environment.
GROQ_API_KEY=
